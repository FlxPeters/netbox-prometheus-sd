[tool.poetry]
name = "netbox-prometheus-sd"
version = "0.1.0"
description = "Servie discovery in Prometheus from Netbox"
authors = ["Felix Peters <felix.peters@breuninger.de>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.7"
pynetbox = "^6.1.2"
prometheus-client = "^0.11.0"
environs = "^9.3.2"
netaddr = "^0.8.0"

[tool.poetry.dev-dependencies]
pytest = "^6.2.4"
black = "^21.5b2"
pytest-cov = "^2.12.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
